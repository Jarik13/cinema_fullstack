
const initialState = {
    hall: null,
    halls: [],
    loading: false,
    error: null,
};

export const hallReducer = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}